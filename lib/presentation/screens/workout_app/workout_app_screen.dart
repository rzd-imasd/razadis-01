import 'package:flutter/material.dart';

class WorkoutAppScreen extends StatelessWidget {
  static const String name = 'workout_app_screen';

  
  const WorkoutAppScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}