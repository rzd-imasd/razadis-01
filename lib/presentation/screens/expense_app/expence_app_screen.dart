import 'package:flutter/material.dart';

class ExpenceAppScreen extends StatelessWidget {
  static const String name = 'expence_app_screen';

  
  const ExpenceAppScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}